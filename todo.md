[x] add a close side bar for the chart to properly display `add a way to collapse the sidebar for a better view of the chart and better responsiveness on all screen sizes`
[ ] i want oreduce market data api call to avoid rate limit, instead of making api calls every time i change the time frame. for example i select a range fo april 2025 - september 2025 it makes and api call to twelve market and saves it in the database and make calls the the database when i change the time frame from let's say 1hr to 5mins
[x] upgrade the trading panel add limit executions and market executions
